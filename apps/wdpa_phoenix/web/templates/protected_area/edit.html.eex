<h2>Edit protected area</h2>

<%= render "form.html", changeset: @changeset,
                        action: protected_area_path(@conn, :update, @protected_area) %>

<%= link "Back", to: protected_area_path(@conn, :index) %>
